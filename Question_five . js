
// 5a. Create a function to sum two numbers and return the value. 
  5b. Create a function to generate a random number between 0 to 10
  5c. Pass two values to the sum function, But first value should be 
    passed by the user and the second value should be generated by a 
    random function //
    
    var a = 10;
let random = function () {
    return Math.floor(Math.random() * 10)
}
function sum(x,y){
    return(x+y)
}

console.log(sum(a,random()))
